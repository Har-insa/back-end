@model IEnumerable<ConnextBusinessLayer.Bdd.REQUEST_TRAVEL>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.ACTION.LIBELLE)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.TRAVEL.ID_TRAVEL)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.USER.FIRST_NAME)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.ACTION.LIBELLE)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.TRAVEL.ID_TRAVEL)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.USER.FIRST_NAME)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ID_REQUEST_TRAVEL }) |
            @Html.ActionLink("Details", "Details", new { id=item.ID_REQUEST_TRAVEL }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ID_REQUEST_TRAVEL })
        </td>
    </tr>
}

</table>
