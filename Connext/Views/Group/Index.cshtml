@model IEnumerable<ConnextBusinessLayer.Bdd.GROUP>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.LABEL)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.LABEL)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ID_GROUP }) |
            @Html.ActionLink("Details", "Details", new { id=item.ID_GROUP }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ID_GROUP })
        </td>
    </tr>
}

</table>
